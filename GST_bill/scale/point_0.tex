\documentclass[a4paper]{article}
\usepackage[top=13.5mm, bottom=13.5mm, left=10mm, right=10mm]{geometry}
\usepackage{tikz}

\begin{document}

\begin{center}
    \begin{tikzpicture}[scale=0.1, anchor=south west] % Align origin to bottom-left
        
        % Set the canvas origin to the bottom-left corner
        \useasboundingbox (0,0) rectangle (190,270);

        % Draw only one point at (0,0) (bottom-left corner)
        \fill (0,0) circle (0.5); 
        \node[scale=0.5, anchor=north east] at (0,0) {(0,0)}; % Label the point
        
        % Draw a line from (0,0) to (100,50)
        \draw (0,0) -- (100,50);
        \fill (100,50) circle (0.5); 
        % Label the endpoint (100,50)
        \node[scale=0.5, anchor=south] at (100,50) {(100,50)};

        %top side
        \draw (0,0) -- (0,10);
        \fill (0,10) circle (0.5); 
        %Down side
        \draw (0,0) -- (0,-10);
        \fill (0,-10) circle (0.5); 
        % Label the endpoint (100,50)
        %\node[scale=0.5, anchor=south] at (100,50) {(100,50)};
        %left side
        \draw (0,0) -- (-10,0);
        \fill (-10,0) circle (0.5); 
        %RIGHT side
        \draw (0,0) -- (10,0);
        \fill (10,0) circle (0.5); 


    \end{tikzpicture}
\end{center}

\end{document}
