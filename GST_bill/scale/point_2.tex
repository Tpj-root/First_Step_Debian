\documentclass[a4paper]{article}
\usepackage[top=13.5mm, bottom=13.5mm, left=10mm, right=10mm]{geometry}
\usepackage{tikz}

\begin{document}

\begin{center}
    \begin{tikzpicture}[scale=0.1, anchor=south west] 
        
        % Set the canvas origin to the bottom-left corner
        \useasboundingbox (0,0) rectangle (190,270);
        
        % Define how many steps you want to move and rotate
        \foreach \i in {0,10,...,200} { % Move every 10mm
            \begin{scope}[rotate around={\i*10:(\i,0)}, shift={(\i,0)}] % Rotate 5° per step, move 10mm per step
                
                % Draw the cross object
                \fill (0,0) circle (0.5); 
                \draw (0,0) -- (0,10);
                \fill (0,10) circle (0.5); 
                \draw (0,0) -- (0,-10);
                \fill (0,-10) circle (0.5); 
                \draw (0,0) -- (-10,0);
                \fill (-10,0) circle (0.5); 
                \draw (0,0) -- (10,0);
                \fill (10,0) circle (0.5); 

            \end{scope}
        }

    \end{tikzpicture}
\end{center}

\end{document}
